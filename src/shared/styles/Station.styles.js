import React from 'react';
import { AppRegistry, StyleSheet } from 'react-native';

import { remCalc } from './Common.styles';

// Card
export default StyleSheet.create({
  base: {
  	color: "#666",
  	textDecorationLine: "underline", // Was border-bottom: 1
    borderColor: "#ddd"
  },
});
